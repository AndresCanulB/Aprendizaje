"use strict";
(() => {
    // null y undefined - Son dos tipos primitivos que representan la ausencia de valor, pero con significados distintos.
    // strictNullChecks
    let isActive = null;
    // console.log(isActive)
})();
/*

null

- Lo asigna el programador explícitamente
- Representa un valor intencional

let user = null (no hay usuario)

undefined

- Lo asigna TypeScript/JavaScript automáticamente
- Representa un valor no inicializado

let name; (olvidé asignar)

*/ 
